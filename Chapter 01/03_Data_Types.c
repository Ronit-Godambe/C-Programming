#include <stdio.h>
int main()
{

    // INTEGER -

    // int a;
    // a = 1;
    int a = 1; // works exactly as the above one
    // ----------------------------------------------------------------

    // FLOAT

    //    float b;
    //    b = 49.13;
    float b = 49.13;

    // CHARACTER - stores only 1 single value

    // char c;
    //  c = 'V'
    char c = 'V';
    char h = '\n';

    // --------------------------------------------------

    printf("the value of a is %d\n", a);
    printf("the value of a is %f\n", b);
    printf("the value of a is %c\n", c);
}