#include <stdio.h>
int main()
{
    int a;       // allowed
    int b;       // allowed
    int c;       // allowed
    int Ronit_g; // allowed

    // variables are case-sensitive - Harry and harry are different variables

    // int Ronit@;     // not allowed
    return 0;
}
