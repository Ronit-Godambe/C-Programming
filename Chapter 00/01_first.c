#include<stdio.h>

int main(){
    printf("Hello World");
    printf("Ronit");
    // to run any c file we always type in the terminal - gcc filename.c (here, gcc 01_first.c)
    // then - a file named a.exe appears automatically - in termninal type - ./a.exe (or any file name)
    return 0;
}